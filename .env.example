# ============================================================================
# Parser Law - Environment Configuration Template
# ============================================================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env:  cp .env.example .env
# 2. Update values with your actual credentials
# 3. NEVER commit .env to git (it's in .gitignore)
# 
# Last Updated: 2025-11-11
# ============================================================================

# ----------------------------------------------------------------------------
# Database Configuration (PostgreSQL)
# ----------------------------------------------------------------------------
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=monitoring_platform
POSTGRES_USER=parser_user
POSTGRES_PASSWORD=change_me_in_production

# Schema to use (default: legislatie)
POSTGRES_SCHEMA=legislatie

# ----------------------------------------------------------------------------
# API Configuration (FastAPI)
# ----------------------------------------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000
API_VERSION=1.0.0
API_TITLE="Parser Law API"
API_DESCRIPTION="API for parsing and managing Romanian legal acts"

# Logging
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# CORS (Comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# ----------------------------------------------------------------------------
# Docker Container Names
# ----------------------------------------------------------------------------
# IMPORTANT: Must match docker-compose.yml
CONTAINER_API=legislatie_api
CONTAINER_DB=legislatie_postgres
CONTAINER_SCHEDULER=legislatie_scheduler

# ----------------------------------------------------------------------------
# VPS Configuration (Production Only)
# ----------------------------------------------------------------------------
VPS_HOST=77.237.235.158
VPS_USER=root
VPS_PATH=/opt/parser-law
VPS_BACKUP_PATH=/opt/parser-law/backups

# ----------------------------------------------------------------------------
# Scraping Configuration
# ----------------------------------------------------------------------------
# Base URL for Romanian Official Gazette (MOF)
MOF_BASE_URL=https://legislatie.just.ro

# Rate limiting (requests per second)
SCRAPER_RATE_LIMIT=2

# Timeout for HTTP requests (seconds)
SCRAPER_TIMEOUT=30

# User agent for requests
SCRAPER_USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"

# ----------------------------------------------------------------------------
# Scheduler Configuration
# ----------------------------------------------------------------------------
SCHEDULER_ENABLED=true
SCHEDULER_INTERVAL_HOURS=24
SCHEDULER_START_TIME=02:00

# ----------------------------------------------------------------------------
# AI Processing Configuration
# ----------------------------------------------------------------------------
# OpenAI API Key (if using AI features)
OPENAI_API_KEY=your_openai_key_here

# AI Model to use
AI_MODEL=gpt-4

# AI Processing batch size
AI_BATCH_SIZE=10

# ----------------------------------------------------------------------------
# File Paths
# ----------------------------------------------------------------------------
# Local development paths (relative to project root)
BACKUP_PATH=./backups
LOG_PATH=./logs
EXPORT_PATH=./export_csv
RESULTS_PATH=./rezultate

# ----------------------------------------------------------------------------
# Email Notifications (Optional)
# ----------------------------------------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM=noreply@issuemonitoring.ro

# Email recipients for alerts (comma-separated)
ALERT_EMAILS=octavian@issuemonitoring.ro

# ----------------------------------------------------------------------------
# Security
# ----------------------------------------------------------------------------
# Secret key for JWT tokens (if implementing auth)
SECRET_KEY=generate_a_random_secret_key_here

# JWT token expiration (minutes)
JWT_EXPIRATION_MINUTES=30

# ----------------------------------------------------------------------------
# Development vs Production
# ----------------------------------------------------------------------------
# Environment: development, staging, production
ENVIRONMENT=development

# Debug mode (true/false)
DEBUG=true

# ----------------------------------------------------------------------------
# Database Connection Pool
# ----------------------------------------------------------------------------
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# ----------------------------------------------------------------------------
# Monitoring & Observability
# ----------------------------------------------------------------------------
# Sentry DSN (error tracking, optional)
SENTRY_DSN=

# Prometheus metrics enabled
PROMETHEUS_ENABLED=false
PROMETHEUS_PORT=9090

# ----------------------------------------------------------------------------
# Feature Flags
# ----------------------------------------------------------------------------
FEATURE_AI_LABELING=true
FEATURE_CATEGORIES=true
FEATURE_DIFF_TRACKING=true
FEATURE_EMAIL_NOTIFICATIONS=false

# ============================================================================
# NOTES:
# ============================================================================
# 
# 1. For local development:
#    - Use localhost for POSTGRES_HOST
#    - Use port 5432 for POSTGRES_PORT
#    - Set DEBUG=true
# 
# 2. For VPS production:
#    - Use container name for POSTGRES_HOST (legislatie_postgres)
#    - Set strong password for POSTGRES_PASSWORD
#    - Set DEBUG=false
#    - Configure email notifications
# 
# 3. Security best practices:
#    - Never commit .env to git
#    - Use different credentials for dev/prod
#    - Rotate passwords regularly
#    - Use environment-specific .env files
# 
# 4. Database connection string format:
#    postgresql://{POSTGRES_USER}:{POSTGRES_PASSWORD}@{POSTGRES_HOST}:{POSTGRES_PORT}/{POSTGRES_DB}
# 
# 5. To generate a secret key:
#    python -c "import secrets; print(secrets.token_urlsafe(32))"
# 
# ============================================================================
